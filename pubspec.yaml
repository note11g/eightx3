name: eightx3
description: 8x3 project

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.17.5 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  camera: ^0.9.8+1
  get: ^4.6.5
  flutter_svg: ^1.1.0
  video_player: ^2.4.5
  decorated_icon: ^1.2.1
  hive: ^2.2.2
  hive_flutter: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  hive_generator: ^1.1.3
  build_runner: ^2.1.11

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/imgs/
